{
  "name": "claude-token-tracker",
  "displayName": "Claude Token Tracker",
  "description": "Track and display Claude Code token usage from local session logs",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "claudeTokenTracker.refreshUsage",
        "title": "Claude Tokens: Refresh Usage"
      },
      {
        "command": "claudeTokenTracker.showDetails",
        "title": "Claude Tokens: Show Details"
      },
      {
        "command": "claudeTokenTracker.openSettings",
        "title": "Claude Tokens: Open Settings"
      }
    ],
    "configuration": {
      "title": "Claude Token Tracker",
      "properties": {
        "claudeTokenTracker.displayUnit": {
          "type": "string",
          "default": "tokens",
          "enum": [
            "tokens",
            "cost_usd",
            "energy_kwh",
            "trees_burned"
          ],
          "enumDescriptions": [
            "Raw token count",
            "Estimated USD cost (per-model pricing)",
            "Estimated energy consumption in kWh",
            "Fun metric: equivalent trees burned"
          ],
          "description": "Unit used to display token usage across all UI surfaces"
        },
        "claudeTokenTracker.showStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show token usage in the status bar"
        },
        "claudeTokenTracker.showSidebar": {
          "type": "boolean",
          "default": true,
          "description": "Show token usage in the sidebar panel"
        },
        "claudeTokenTracker.pricing.opus.input": {
          "type": "number",
          "default": 15.00,
          "description": "Opus input token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.opus.output": {
          "type": "number",
          "default": 75.00,
          "description": "Opus output token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.opus.cacheCreation": {
          "type": "number",
          "default": 18.75,
          "description": "Opus cache creation token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.opus.cacheRead": {
          "type": "number",
          "default": 1.50,
          "description": "Opus cache read token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.sonnet.input": {
          "type": "number",
          "default": 3.00,
          "description": "Sonnet input token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.sonnet.output": {
          "type": "number",
          "default": 15.00,
          "description": "Sonnet output token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.sonnet.cacheCreation": {
          "type": "number",
          "default": 3.75,
          "description": "Sonnet cache creation token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.sonnet.cacheRead": {
          "type": "number",
          "default": 0.30,
          "description": "Sonnet cache read token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.haiku.input": {
          "type": "number",
          "default": 0.80,
          "description": "Haiku input token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.haiku.output": {
          "type": "number",
          "default": 4.00,
          "description": "Haiku output token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.haiku.cacheCreation": {
          "type": "number",
          "default": 1.00,
          "description": "Haiku cache creation token price per million tokens (USD)"
        },
        "claudeTokenTracker.pricing.haiku.cacheRead": {
          "type": "number",
          "default": 0.08,
          "description": "Haiku cache read token price per million tokens (USD)"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "claudeTokenTracker",
          "title": "Claude Tokens",
          "icon": "resources/claude-icon.svg"
        }
      ]
    },
    "views": {
      "claudeTokenTracker": [
        {
          "id": "claudeTokenTracker.usageView",
          "name": "Token Usage",
          "type": "webview",
          "when": "claudeTokenTracker.showSidebar"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.109.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.54.0",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
